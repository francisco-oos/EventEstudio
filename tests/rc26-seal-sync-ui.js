"use strict";
const assert=require("assert");
const fs=require("fs");
const path=require("path");
const root=path.join(__dirname,"..");
const read=file=>fs.readFileSync(path.join(root,file),"utf8");
const adminHtml=read("public/admin.html");
const admin=read("public/admin.js");
const app=read("public/app.js");
const styles=read("public/styles.css");
const index=read("public/index.html");
assert(adminHtml.includes('id="sealPreviewThumb"'));
assert(adminHtml.includes('id="sealSettingsAdvanced"'));
assert(admin.includes('function syncSealAdminVisibility()'));
assert(admin.includes('function refreshSealPreview()'));
assert(admin.includes('openingSupportsSeal('));
assert(index.includes('id="heroWaxSeal"'));
assert(app.includes('function renderFinalHeroSeal(style)'));
assert(app.includes("$('openingDate').classList.toggle('hidden',style==='particle-heart');"));
assert(styles.includes('.hero-wax-seal'));
assert(styles.includes('.seal-settings-summary'));
assert(styles.includes('.invitation-opening.is-opening .opening-card{transform:translateY(-48%);z-index:5}'));
console.log('✓ RC26+: panel de sello contextual y sello final reutilizable presentes');
